# CONTENT FLOW — Comparing Decimal Numbers (Updated) 

---

## 1. ENTRY + CONCEPT HOOK

### Step 1

* Display:

  * `Let’s compare these decimal numbers.`
  * `68.345    39.836`
* Supporting text:

  * `We compare decimals using place values.`
* CTA:

  * `Tap to begin`
* Visual cue:

  * Pulsating arrow

---

## 2. ALIGNMENT FLOW (DRAG INTERACTION)

### Step 2 — First Number Drag

* Instruction:

  * `Write the numbers so the decimal points are one below the other.`
* Display:

  * `T O t h th`
  * `68.345    39.836`
* Interaction:

  * Drag first number
* Behavior:

  * Numbers appear in box one after the other
  * Each place pulsates
* Visual cue:

  * First row pulsates

---

### Step 3 — Second Number Drag

* Display:

  * `6 8 3 4 5`
  * `68.345    39.836`
* Instruction:

  * `Write the numbers so the decimal points are one below the other.`
* Interaction:

  * Drag second number
* Behavior:

  * Numbers appear in box one after the other
  * Each place pulsates
* Visual cue:

  * Second row pulsates

---

### Step 4 — Decimal Alignment Confirmation

* Display:

  * `6 8 3 4 5`
  * `3 9 8 3 6`
  * `68.345    39.836`
* Instruction:

  * `Write the numbers so the decimal points are one below the other.`
* Visual cue:

  * Decimal points pulsate
* Auto-transition:

  * Move after 2 seconds

---

## 3. GUIDED COMPARISON (FIRST EXAMPLE)

### Step 5 — First Comparison Prompt

* Instruction:

  * `Start comparing from the leftmost digit.`
* Display:

  * `6 3`
  * Options: `<   =   >`
* Visual cue:

  * Pulsate tens place digits (`6` and `3`)
  * Pulsate options
* Error behavior:

  * Incorrect option → teeter

---

### Step 6 — Correct Comparison

* Display:

  * `6 > 3`
* Instruction:

  * `Start comparing from the leftmost digit.`
* Behavior:

  * Symbol moves to decimal numbers

---

### Step 7 — Final Result + Rule

* Display:

  * `68.345 > 39.836`
  * `6 > 3`
* Statements:

  * `The number with the greater first different digit is larger.`
  * `The first different digit decides the greater number.`
* Visual cue:

  * Pulsating arrow

---

## 4. PRACTICE SET (SCROLLABLE)

### Step 8 — Problem Pool

* Instruction:

  * `Try comparing these decimal numbers.`
* Display list:

  * `25.678 ? 32.789`
  * `? 82.905 83.999`
  * `?`
  * `? 8.345 8.349`
  * `?`
  * `19.56 19.52`
  * `64.305 64.209`
  * `7.891 ? 7.631`
  * `75.536 ? 96.745`
* CTA:

  * `Scroll to choose`
  * `Compare`
* Visual cue:

  * Pulsating decimal points during scroll (indicating alignment)

---

## 5. WORKED PRACTICE 1 (25.678 vs 32.789)

### Step 9 — First Digit Comparison

* Display:

  * `2 3`
  * Options: `<   =   >`
* Visual cue:

  * Pulsate `2` and `3`

---

### Step 10 — Selection Result

* Display:

  * `2 < 3`
* Behavior:

  * Conclusion appears in next slide
  * Copy of symbol moves to question mark

---

### Step 11 — Final Result + Rule

* Display:

  * `25.678 < 32.789`
  * `2 < 3`
* Statement:

  * `The number with the greater first different digit is larger.`
* Visual cue:

  * Pulsate `2` and `3`

---

## 6. WORKED PRACTICE 2 (82.905 vs 83.999)

### Step 12 — Setup

* Display:

  * `8 2 9 0 5`
  * `8 3 9 9 9`
  * `82.905 ? 83.999`
* Instruction:

  * `Try comparing these decimal numbers.`
* CTA:

  * `Scroll to choose`
  * `Compare`

---

### Step 13 — First Digit Comparison

* Display:

  * `8 8`
  * Options: `<   =   >`

---

### Step 14 — Equality Case

* Display:

  * `8 = 8`
* Behavior:

  * Next slide appears
  * Comparison box shifts to next place (ones place)

---

### Step 15 — Rule for Equality

* Statement:

  * `If digits are equal, move to the next place and compare.`

---

### Step 16 — Next Digit Comparison

* Display:

  * `2 3`
  * Options: `<   =   >`
* Visual cue:

  * Pulsate `2` and `3`

---

### Step 17 — Selection Result

* Display:

  * `2 < 3`
* Visual cue:

  * Pulsate `2` and `3`

---

### Step 18 — Final Result + Rule

* Display:

  * `82.905 < 83.999`
  * `2 < 3`
* Statement:

  * `The first different digit decides which number is greater.`
* Behavior:

  * Symbol moves to question mark box in list

---

## 7. EXTENDED RULE (DEEP CASE)

### Step 19

* Display:

  * `8 2 6 0 5`
  * `8 2 7 9 9`
* Statement:

  * `Keep comparing until the digits are different.`
* Instruction:

  * `Try comparing these decimal numbers.`

---

## 8. VARIATION SET (CONTENT POOL)

### Place Value Variations

* Tens are different
* Tens are same and ones are different
* Tens are same, ones are same, tenths are different
* Tens are same, ones are same, tenths are same, hundredths are different
* Tens are same, ones are same, tenths are same, hundredths are same, thousandths are different

---

### Starting Point Variations

* When the decimal starts from tens place
* When the decimal starts from ones place

---

### Ones-Based Variations

* Ones are different
* Ones are same, tenths are different
* Ones are same, tenths are same, hundredths are different

---

## 9. EDGE CASE SET

* `When the decimal places has zeros`
* `When the decimal places has no thousandths`
* `Equal decimal numbers – 5.230 = 5.23`
* `Numbers less than 1 – 0.45 vs 0.405`
* `Different number of decimal places – 3.4 vs 3.40`
* `Edge case with zeros inside – 2.305 vs 2.35`

---

## 10. FINAL SUMMARY STATE

### Summary:

* `Align the decimal points.`

### Steps to compare decimals:

* `Compare digits from the leftmost place.`
* `The first different digit tells which number is greater.`

---

## KEY DELTA FROM PREVIOUS VERSION (for your clarity)

* Added **Tap to begin entry gate**
* Added **explicit drag feedback behavior (digit-by-digit pulsation)**
* Added **interactive symbol selection (< = >)**
* Added **error behavior (teeter)**
* Added **symbol movement into result**
* Added **equality handling flow (shift place comparison)**
* Added **"Keep comparing until digits differ" rule**
* Stronger **scroll + alignment visual coupling**

---
